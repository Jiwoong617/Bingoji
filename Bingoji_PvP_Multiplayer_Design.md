# Bingoji PvP 멀티플레이 기획서

> 문서 상태: 확정 기획안 · Local PvP 전체 흐름 구현·검증 완료 · Cloudflare Production 배포 전  
> 대상 모드: 방 코드 기반 1:1 친선 PvP  
> 목표: 기존 싱글플레이를 변경하지 않고, 같은 5×5 Bingo 규칙을 두 플레이어가 공정하게 사용하는 실시간 대전 모드를 추가한다.  
> 비고: Cloudflare Worker 배포와 GitHub Actions의 Worker URL 변수 연결만 운영 단계로 남아 있다.

## 1. 핵심 방향

- 게임 시작 전 `싱글플레이`와 `멀티플레이`를 선택한다.
- 싱글플레이를 선택하면 현재의 캐릭터 선택, 난이도 선택, Map 진행, 이벤트, 보상, Boss 흐름을 그대로 사용한다.
- 멀티플레이는 캐릭터 능력과 Run 진행 없이, 플레이어가 정한 Avatar와 Pool로 한 판의 1:1 대전을 진행한다.
- 두 플레이어는 동일한 HP, Draw 규칙, 행동 시간과 Board 규칙을 사용한다.
- 방 생성과 참가에는 짧은 방 코드를 사용한다. 공개 Matchmaking, Ranking, 관전, Chat은 첫 구현 범위에 포함하지 않는다.
- 방, Turn, Timer, Random, 효과 처리와 승패 판정은 서버가 최종 결정한다. 클라이언트는 입력과 연출만 담당한다.

## 2. 전체 사용자 흐름

```text
메인 화면
  └─ 모드 선택
      ├─ 싱글플레이
      │   └─ 기존 캐릭터 선택 → 난이도 선택 → 기존 Run
      │
      └─ 멀티플레이
          └─ 프로필·Pool 설정
              ├─ 방 만들기 ─┐
              └─ 방 참가 ───┴→ 대기방 → PvP 전투 → PvP 결과 → 메인 화면
```

멀티플레이 화면에서 뒤로 가기를 누르면 메인 화면으로 돌아간다. 전투 도중 나가기는 단순 뒤로 가기가 아니라 기권으로 처리한다.

## 3. 모드 선택 화면

메인 화면의 `게임 시작` 이후, 캐릭터 선택보다 먼저 표시한다.

| 선택 | 설명 | 다음 화면 |
|---|---|---|
| 싱글플레이 | 기존 PvE Run | 기존 캐릭터 선택 |
| 멀티플레이 | 방 코드 기반 1:1 대전 | 멀티플레이 프로필·Pool 설정 |

- 기존 저장 중인 싱글플레이 Run이 있다면 싱글플레이 쪽에서만 이어하기를 제공한다.
- 멀티플레이 닉네임, Avatar, Pool은 DB뿐 아니라 `localStorage`, `sessionStorage`, Cookie, IndexedDB에도 저장하지 않는다.
- 플레이어 정보 초안은 현재 Web Page Runtime의 App Memory에 유지한다. 메인 화면으로 돌아가거나 결과 화면을 닫아도 같은 Page가 살아 있다면 Avatar, 닉네임과 Pool을 보존한다.
- 같은 Page에서 멀티플레이를 다시 선택하면 직전에 설정한 Avatar, 닉네임과 Pool을 미리 채워 보여주며, 그대로 사용하거나 수정할 수 있다.
- 새로고침, Tab·Browser 종료 후 재접속처럼 Web Page가 새로 불릴 때만 이전 플레이어 정보와 Session Token을 초기화한다.
- 멀티플레이 전적, 계정, 최근 Profile과 최근 Pool 저장은 범위에서 제외한다.

## 4. 멀티플레이 프로필

### 4.1 플레이어 정보

플레이어는 다음 두 항목을 정한다.

- `Avatar Emoji`: 현재 Avatar를 누르면 채팅 앱과 같은 검색·분류 Grid Picker가 열리며, 전체 Unicode 공용 Emoji 중 하나를 선택한다.
- `닉네임`: 상대와 결과 화면에 표시할 이름을 입력한다.

Avatar는 외형만 나타내며 전투 능력이나 Pool에 영향을 주지 않는다. Board에 사용하는 Emoji와도 별개의 값이다.

### 4.2 입력 규칙

- 닉네임은 앞뒤 공백을 제거한 뒤 2~12자로 제한한다.
- 공백과 한글, 영문, 숫자는 허용한다.
- 완전히 공백뿐인 이름과 화면 배치를 깨뜨릴 수 있는 제어 문자는 허용하지 않는다.
- 같은 방에서 닉네임이 같더라도 참가할 수 있다. 화면에는 `나`와 `상대` 표기를 함께 사용해 구분한다.
- 욕설 필터와 로그인 기반 고유 닉네임은 첫 구현 범위에서 제외한다.

닉네임 또는 Avatar가 유효하지 않으면 `방 만들기`와 `방 참가`를 비활성화하고 가까운 위치에 이유를 표시한다.

## 5. PvP Pool 구성

### 5.1 기본 규칙

Pool의 수량은 Emoji 종류 수가 아니라 **사본 수의 합계**로 계산한다.

| 항목 | 제한 |
|---|---:|
| 전체 Pool | 최소 10개, 최대 15개 |
| 같은 Emoji | 최대 2개 |
| 일반 | 전체 수량 제한 없음 |
| 고급 | 최대 8개 |
| 희귀 | 최대 4개 |

예를 들어 같은 희귀 Emoji 2개와 다른 희귀 Emoji 2개를 넣었다면 희귀 한도 4개를 모두 사용한 것이다.

### 5.2 선택 가능한 Emoji

- PvP 목록은 별도 수기 목록이 아니라 현재 Emoji Registry에서 자동 생성한다.
- 싱글플레이의 `캐릭터 전용` Emoji도 PvP에서는 특정 캐릭터 없이 선택할 수 있다.
- Emoji의 등급, 이름, 설명, Bingo 효과, 배치 효과, 보유 중 효과를 모두 확인할 수 있어야 한다.
- 새 전투용 Emoji가 Registry에 추가되면 별도의 PvP 화면 수정 없이 선택 목록에도 나타나야 한다.

현재 Registry에는 전투 보상용 Emoji 외에 `🥚 품고 있는 알(event_egg)`과 `👶 맡겨진 아기(event_baby)`처럼 이벤트 진행을 위해 만든 임시 Emoji도 포함되어 있다. 이 둘은 Bingo 효과가 없고 이벤트 문맥이 없으면 기능하지 않으므로 **PvP 선택 목록에서 제외한다.** 그 외 현재 구현된 모든 전투용 Emoji는 선택할 수 있다.

### 5.3 Pool 편집 UI

- Emoji 카탈로그의 항목을 누르면 Pool을 변경하지 않고 해당 Emoji의 정보만 표시한다.
- 정보 영역의 `추가`와 `제거` 버튼으로 선택한 Emoji 사본 수를 조절한다.
- 현재 Pool 영역의 `전체 초기화` 버튼은 선택한 모든 사본을 한 번에 제거한다.
- 카탈로그는 고정된 표시 높이 안에서 마우스 Wheel, Touch Drag와 Scrollbar로 상하 이동한다. 데스크톱에서는 카탈로그와 정보·현재 Pool을 나란히 보여준다.

### 5.4 Pool 편집 화면

화면은 다음 정보를 한눈에 보여준다.

- 상단: Avatar, 닉네임
- 중앙 위: `10 / 15`처럼 현재 Pool 사본 수
- 중앙: 등급이나 테마로 정렬·필터할 수 있는 전체 Emoji 목록
- 선택한 Emoji: 현재 사본 수 `0 / 2`, 등급, 능력 설명
- 하단: 선택된 Pool 전체와 `일반 / 고급 / 희귀` 사용 수
- 최하단: `방 만들기`, `방 참가`

Emoji를 누르면 먼저 정보가 표시되고, 정보 영역의 추가·제거 버튼으로 사본을 조절한다. 최대치에 도달한 Emoji나 등급은 더 추가할 수 없으며 그 이유를 표시한다.

### 5.5 유효성 검사

아래 조건을 모두 만족해야 방을 만들거나 참가할 수 있다.

1. Pool이 10~15개다.
2. 같은 Emoji가 2개를 넘지 않는다.
3. 고급 Emoji가 8개를 넘지 않는다.
4. 희귀 Emoji가 4개를 넘지 않는다.
5. 모든 Emoji ID가 현재 PvP 사용 가능 Registry에 존재한다.

클라이언트는 편집 즉시 안내하지만, 방 준비와 전투 시작 직전에는 서버가 같은 규칙으로 다시 검사한다. 클라이언트 데이터를 조작한 잘못된 Pool은 전투에 사용할 수 없다.

## 6. 방 만들기와 참가

### 6.1 방 만들기

`방 만들기`를 누르면 다음 순서로 진행한다.

1. 서버가 프로필과 Pool을 검증한다.
2. 겹치지 않는 방 코드를 만든다.
3. 플레이어를 Host로 대기방에 입장시킨다.
4. 대기방 상단에 방 코드와 복사 버튼을 표시한다.
5. 중앙 왼쪽에는 Host 정보, 오른쪽에는 `상대를 기다리는 중…` 빈칸을 표시한다.

방 코드는 모바일에서도 전달하기 쉬운 6자리 영문 대문자·숫자 조합으로 만든다. `0/O`, `1/I`처럼 혼동하기 쉬운 문자는 제외한다.

### 6.2 방 참가

`방 참가`를 누르면 방 코드 입력창을 표시한다.

1. 입력값의 공백을 제거하고 영문은 대문자로 변환한다.
2. 서버가 방의 존재 여부와 참가 가능 상태를 확인한다.
3. 참가할 수 있으면 Guest로 대기방에 입장한다.
4. 존재하지 않거나 만료된 코드라면 `존재하지 않는 방코드입니다.` 팝업을 표시한다.

추가 오류는 서로 구분해 표시한다.

- 이미 두 명이 있는 방: `이미 가득 찬 방입니다.`
- 이미 시작한 방: `이미 게임이 시작된 방입니다.`
- 네트워크 실패: `서버에 연결할 수 없습니다. 다시 시도해 주세요.`

오류 팝업을 닫으면 입력한 코드를 수정해 다시 제출할 수 있다.

### 6.3 방 상태

방은 다음 상태 중 하나만 가진다.

```text
waiting → ready → starting → in-game → finished → closed
```

- 한 방에는 Host와 Guest 두 명만 들어갈 수 있다.
- 대기 중인 방은 생성 후 30분이 지나면 만료시킨다.
- 한 플레이어가 중복 참가 요청을 보내도 같은 자리를 두 번 차지하지 않는다.
- 두 플레이어가 동시에 준비하더라도 전투는 한 번만 생성한다.

## 7. 대기방

### 7.1 화면 구성

- 상단: `ROOM CODE`와 방 코드, 복사 버튼
- 중앙 왼쪽: 내 Avatar, 닉네임, Pool 수량, 준비 상태
- 중앙 오른쪽: 상대 Avatar, 닉네임, Pool 수량, 준비 상태 또는 빈칸
- 하단: `준비`와 `방 나가기`

상대의 정확한 Pool 구성은 대기방에서 보여주지 않는다. 미리 상대 Pool을 보고 대응 Pool로 바꾸는 문제를 막기 위해 `Pool 12개 · 구성 완료` 정도만 표시한다. 정확한 Pool은 결과 화면에서 공개한다.

### 7.2 준비

- 상대가 들어오기 전에도 Host는 준비 상태를 켤 수 있다.
- `준비`를 누르면 자신의 프로필과 Pool을 잠근다.
- 설정을 바꾸고 싶다면 `준비 취소` 후 수정한다.
- 두 플레이어가 모두 준비 상태이고 서버 검증을 통과하면 `게임이 시작됩니다.`와 3·2·1 Countdown을 양쪽에 표시한다.
- Countdown은 서버 시각을 기준으로 3초 동안 진행하고, 끝난 뒤 서버가 Match와 첫 15초 Deadline을 생성한다.
- 시작 직전 서버는 두 Pool, 참가자 수, 방 상태를 다시 검사한다.

### 7.3 방 나가기

- Guest가 나가면 Guest 자리는 다시 빈칸이 되고 Host의 준비 상태를 해제한다.
- Host가 나가면 방을 닫고 Guest에게 `방장이 방을 나갔습니다.`를 표시한 뒤 멀티플레이 준비 화면으로 돌려보낸다.
- 대기방에서 나가는 것은 패배나 전적으로 기록하지 않는다.

## 8. PvP 전투 기본 규칙

### 8.1 대칭 규칙

| 규칙 | 나 | 상대 |
|---|---:|---:|
| 시작 HP | 30 | 30 |
| 최대 HP | 30 | 30 |
| 시작 Board 배치 | 없음 | 없음 |
| 기본 Draw | 자신의 Pool에서 3개 | 자신의 Pool에서 3개 |
| 기본 배치 | 자기 Turn에 1개 | 자기 Turn에 1개 |
| 행동 제한 시간 | 15초 | 15초 |

- 멀티플레이에서는 싱글플레이 캐릭터의 HP와 Ability를 사용하지 않는다.
- Avatar에는 능력이 없다.
- 시작 Board는 25칸 모두 빈칸이다. 기존 PvE의 플레이어 3개·적 3개 무작위 초기 배치를 호출하지 않는다.
- 선공은 서버의 seed 기반 Random으로 50:50 결정하고 양쪽에 동시에 공개한다.
- 전투 화면 진입 시 `당신은 선공입니다` 또는 `당신은 후공입니다` 안내를 잠깐 표시한다.
- 두 플레이어는 자신의 Pool에서 동일하게 3개를 비복원 Draw한다. 다음 자기 Turn에는 원래 전체 Pool에서 다시 Draw한다.
- 사용하지 않은 Draw Emoji는 현재 싱글플레이처럼 쓰레기통 연출 후 사라지며, 영구 Pool에서는 제거되지 않는다.

선공이 빈 Board에 먼저 놓는 구조가 실제 승률에 영향을 줄 수 있으므로, 첫 밸런스 지표로 선공·후공 승률을 반드시 기록한다. 초기 버전에서는 임의의 선공 보정 효과를 추가하지 않는다.

### 8.2 Turn 진행

```text
서버가 현재 행동 플레이어와 Draw 3개 확정
  → 양쪽 화면에 Turn 시작 표시
  → 행동 플레이어에게만 Board·Draw 입력 허용
  → 15초 안에 Emoji와 빈칸 선택·배치
  → 서버가 행동 유효성 검사
  → 배치 효과 → Bingo 판정 → 효과 합산·적용 → 제거
  → 미사용 Draw 폐기
  → 승패 확인
  → 상대 Turn
```

- 상대 Turn에는 내 Board 입력을 잠그되, Board와 Emoji 정보 확인은 허용한다.
- 상대가 고른 Cell은 서버가 배치를 승인한 뒤 현재 적 AI 예고 연출처럼 잠깐 강조하고 Emoji를 놓는다.
- Hover나 아직 확정하지 않은 선택 Cell은 상대에게 전송하지 않는다.
- Bingo 처리 중에는 양쪽 입력과 Turn Timer를 멈춘다. 다음 결정 가능 시점에 서버가 새 15초 Deadline을 보낸다.
- 제거되는 Bingo Cell은 별표로 대체하지 않고, 연출 중에는 제거 전 원본 Emoji를 잔상으로 표시한다.
- 기존의 가로 → 세로 → 대각선 Bingo 순서, 효과 합산, 임시 유지 Emoji, 교차 Emoji 재발동 규칙을 그대로 사용한다.
- 한 Line의 Emoji 소유자가 섞여 있어도, 마지막 칸을 놓아 Bingo를 완성한 플레이어가 그 Line의 모든 효과를 자신의 관점에서 사용한다.
- `보유 중 효과`는 Board 소유권과 관계없이 해당 Emoji를 자기 Pool에 가진 플레이어에게만 적용한다.

### 8.3 추가 배치 효과

기존 `한 번 더` 계열 효과도 PvP에서 사용할 수 있다.

- 추가 배치는 같은 Turn에 남은 Draw Emoji 중 하나를 사용한다.
- 추가 배치 기회마다 새로운 15초 결정 시간을 준다.
- 추가 배치 효과를 마지막 Draw로 발동했다면 기존 규칙처럼 새 Emoji를 뽑지 않고 종료한다.
- 추가 배치 연쇄는 현재 MVP 정책과 동일하게 허용하지 않는다.
- 추가 배치 여부와 남은 횟수도 서버 상태가 기준이다.

## 9. 15초 Timer와 자동 배치

### 9.1 Timer 표시

- 행동 플레이어와 상대 모두 같은 남은 시간을 본다.
- 15초부터 숫자와 원형 Gauge가 감소한다.
- 5초 이하에서는 색상뿐 아니라 짧은 Pulse와 숫자로 긴박함을 알린다.
- 서버가 보낸 절대 Deadline을 기준으로 계산해 기기 성능이나 Browser Tab 상태에 따른 차이를 줄인다.
- 효과 처리, Bingo 연출, 서버 재동기화 중에는 결정 시간이 흐르지 않는다.

### 9.2 시간 초과

15초 안에 서버가 유효한 배치를 받지 못하면 서버가 자동 행동을 만든다.

1. 현재 남은 Draw 중 하나를 균등 Random으로 선택한다.
2. 현재 배치 가능한 빈칸 중 하나를 균등 Random으로 선택한다.
3. 일반 배치와 똑같이 배치 효과와 Bingo를 처리한다.
4. 화면에 `시간 초과 · 자동 배치`를 표시한다.
5. 효과 처리가 끝나면 Turn을 넘긴다.

자동 배치는 클라이언트의 `Math.random()`이 아니라 서버의 재현 가능한 RNG를 사용한다. 플레이어가 화면에서 Cell이나 Emoji를 선택만 하고 확정된 배치를 보내지 않았다면 그 선택을 보존하지 않고 위 Random 규칙을 사용한다. 이렇게 해야 네트워크 지연과 클라이언트별 입력 상태가 결과를 바꾸지 않는다.

자동 배치도 모든 배치/Bingo 효과를 정상 발동한다. 전략적으로 좋은 칸을 보장하지 않는 것이 시간 초과의 불이익이다.

## 10. Emoji 효과의 PvP 해석

현재 효과의 `자신`, `상대`, `player`, `enemy` 표현은 PvP에서 다음처럼 해석한다.

| 효과 표현 | PvP 대상 |
|---|---|
| 자신 / owner | 효과를 발동한 플레이어 |
| 상대 / opponent | 다른 플레이어 |
| 내 Pool에 보유 중 | 해당 Emoji를 자기 Pool에 넣은 플레이어 |
| 적에게 피해 | 상대 플레이어에게 피해 |
| 자신 회복·방어막 | 발동 플레이어에게 적용 |

UI 문구에서는 PvE의 `적` 대신 `상대`를 사용한다. 엔진에서도 장기적으로 `player/enemy`에 직접 의존하기보다 두 참가자 ID와 `self/opponent` 관계로 효과 대상을 계산해야 한다.

추가 점검이 필요한 효과는 다음과 같다.

- 캐릭터 ID를 참조해 변신 대상이나 전용 보상 Pool을 찾는 효과
- 이벤트 Modifier나 특정 적 Ability를 전제로 하는 효과
- Random 대상을 현재 공용 Emoji 목록에서 뽑는 효과
- 자기 피해로 양쪽 HP가 동시에 0이 될 수 있는 효과
- 한쪽 클라이언트에서만 Random을 다시 계산하는 효과

PvP에서는 같은 입력과 서버 seed가 항상 같은 결과를 만들어야 한다. Random 변신은 PvP 사용 가능 Registry를 기준으로 서버가 한 번만 결정하고 양쪽에 같은 결과를 보낸다.

## 11. 전투 화면

기존 싱글플레이 전투 화면과 Board, Draw, Pool, 쓰레기통, Bingo 연출 구조를 재사용한다.

변경되는 정보는 다음과 같다.

- `적` 영역 → `상대`의 Avatar, 닉네임, HP, 상태 효과
- `플레이어` 영역 → `나`의 Avatar, 닉네임, HP, 상태 효과
- Stage와 Map 진행 → 방 코드 또는 `PvP`, Turn 수, 현재 행동 플레이어, Timer
- 적 AI 예고 → 상대의 서버 승인 배치 예고
- 전투 나가기 → `기권하고 나가시겠습니까?` 확인

상단에는 두 플레이어의 HP를 동시에 비교할 수 있게 나란히 표시한다. 현재 행동 중인 플레이어 정보와 Board 외곽을 함께 강조해 색상만 보지 않아도 Turn을 알 수 있게 한다.

네트워크가 잠시 끊기면 화면 위에 재연결 상태를 표시하고 Board 입력을 잠근다. 마지막으로 서버가 승인한 상태를 다시 받은 뒤에만 입력을 재개한다.

## 12. 승패와 결과 화면

다음 조건에서 결과 화면으로 이동한다.

- 상대 HP가 0이 되면 승리
- 내 HP가 0이 되면 패배
- 같은 효과 처리 단위에서 양쪽 HP가 모두 0이 되면 무승부
- 상대가 전투 중 `나가기`를 선택하거나 재접속 유예 시간을 넘기면 기권승
- 내가 전투 중 나가면 기권패

결과 화면에는 다음을 표시한다.

- `승리`, `패배` 또는 `무승부`
- 나의 Avatar, 닉네임, 최종 HP, Pool
- 상대 Avatar, 닉네임, 최종 HP, Pool
- 총 Turn 수와 경과 시간
- 선공 플레이어
- `닫기` 버튼

`닫기`를 누르면 방 연결과 전투 상태를 정리한 뒤 메인 화면으로 돌아간다. Avatar, 닉네임과 Pool 초안은 현재 Page의 App Memory에 남긴다. 초기 구현에서는 같은 방 재대결 버튼을 제공하지 않는다.

양쪽이 같은 효과 처리 단위에서 동시에 HP 0이 되면 다른 효과 순서로 승자를 만들지 않고 `무승부`로 확정한다.

## 13. 연결 종료와 예외 처리

온라인 대전에서는 정상 Turn 외의 상태도 반드시 처리해야 한다.

### 13.1 연결 종료 정책

- 전투 중 연결이 끊겨도 방과 전투 상태는 서버에 유지한다.
- Web Page가 유지된 상태에서 일시적으로 WebSocket만 끊겼다면 Memory에 있는 같은 참가자 Token으로 30초 동안 자동 재접속할 수 있게 한다.
- 새로고침하거나 Page를 닫으면 Token도 폐기한다. 새로 열린 Page에서는 기존 방이나 Match를 복구하지 않고 멀티플레이 정보를 다시 설정한다.
- 연결이 끊긴 플레이어의 현재 Turn Timer는 계속 진행하며, 만료되면 자동 배치한다.
- 30초 안에 돌아오지 않으면 해당 플레이어의 기권패로 종료한다.
- 서버 연결 자체가 양쪽 모두에게 끊긴 경우에는 승패를 임의 판정하지 않고 복구 또는 경기 무효 상태로 둔다.
- 중복 Tab이나 재전송된 행동은 Turn 번호와 행동 번호로 한 번만 처리한다.

### 13.2 동기화 오류 방지

각 서버 상태에는 증가하는 `revision`을 둔다.

- 배치 요청에는 `roomId`, `matchId`, `turn`, `revision`, `emojiId`, `cellIndex`를 포함한다.
- 서버는 현재 행동 플레이어, Deadline, Draw 보유 여부, 빈칸 여부를 검사한다.
- 오래된 revision이나 중복 요청은 거절하고 최신 상태를 다시 보낸다.
- 클라이언트는 서버 승인 전에 HP, Bingo, 승패를 확정하지 않는다.
- 효과 Event 목록도 서버 결과를 사용해 양쪽이 같은 순서로 연출한다.

## 14. 서버가 필요한 이유와 책임

현재 프로젝트처럼 정적 Web만 배포하면 두 Browser가 같은 방과 Turn 상태를 안전하게 공유할 수 없다. PvP에는 방 상태와 실시간 메시지를 보관할 Backend가 필요하다.

서버의 최소 책임은 다음과 같다.

- 방 코드 생성, 참가, 퇴장, 만료
- 현재 Page Memory에서만 사용하는 플레이어 Session Token과 일시적 WebSocket 재접속
- 프로필·Pool 검증
- 준비 상태와 한 번뿐인 전투 시작
- 선공, Draw, 자동 배치, 변신 등 모든 Random 결정
- 현재 Turn과 15초 Deadline 관리
- 배치 유효성 검사와 Bingo/효과/HP/승패 처리
- 최신 전투 Snapshot과 Effect Event 전송

초기 Backend는 **Cloudflare Workers + Durable Objects(SQLite Storage) + WebSocket Hibernation API**로 확정한다.

- 한 방을 하나의 Durable Object가 소유해 두 참가자, 준비 상태, Match Snapshot, RNG, Turn과 Deadline을 순서대로 처리한다.
- WebSocket Hibernation을 사용해 방이 조용할 때도 연결을 유지하면서 실행 비용을 줄인다.
- Durable Objects는 Workers Free Plan에서도 SQLite Storage 방식으로 사용할 수 있으므로 초기 무료 배포에 적합하다.
- React/Vite Frontend는 기존 정적 Hosting을 유지하고 Cloudflare Worker의 HTTPS/WSS 주소에 연결한다.
- 무료 한도를 넘으면 요청이 실패할 수 있으므로 접속자 수, WebSocket Message 수와 오류율을 기록한다.

근거: [Cloudflare Durable Objects 요금 및 Free Plan](https://developers.cloudflare.com/durable-objects/platform/pricing/), [WebSocket Hibernation 공식 문서](https://developers.cloudflare.com/durable-objects/best-practices/websockets/)

## 15. 기존 시스템과의 경계

### 그대로 재사용할 것

- 5×5 Board와 12개 Line 정의
- Bingo 완성 및 처리 순서
- Emoji Registry와 효과 명령
- 피해, 회복, 방어막, 상태, 임시 유지 등의 중앙 효과 처리 규칙
- Pool 가중 Draw 원칙
- Board, Draw, Pool 정보 UI와 Bingo 연출
- 접근성 및 `prefers-reduced-motion` 정책

### PvP용으로 분리할 것

- 싱글플레이 Run과 PvP Match 상태
- 캐릭터/적 대신 두 Player Slot을 다루는 참가자 모델
- 적 AI 대신 원격 플레이어 입력과 Timer 자동 행동
- PvE 시작 6칸 배치 대신 빈 Board 생성
- Stage, Difficulty, Map, Reward, Event가 없는 PvP 화면 흐름
- 서버 권위 RNG와 상태 revision
- 접속, 대기방, 준비, 재연결, 기권 상태

싱글플레이 코드를 PvP 요구에 맞추기 위해 곳곳에서 조건문으로 바꾸지 않는다. 공용 전투 규칙을 추출하고, PvE와 PvP가 각자의 세션 상태와 Turn 조정자를 사용하게 한다.

## 16. 초기 구현 범위

### 반드시 포함

- 모드 선택
- 멀티플레이 Avatar·닉네임 설정
- 10~15개 Pool Builder와 등급·중복 제한
- 방 만들기, 방 코드 표시·복사
- 방 코드 참가와 오류 안내
- 2인 대기방, 준비/취소, 나가기
- 빈 Board, HP 30, Random 선공의 1:1 대전
- 양쪽 Draw 3개와 15초 Timer
- 시간 초과 서버 자동 배치
- 기존 Bingo 및 Emoji 효과
- 연결 복구 또는 명시적인 기권 처리
- 승리·패배 결과와 메인 화면 복귀

### 첫 구현에서 제외

- 공개 Matchmaking
- 친구 목록과 초대 알림
- 계정, 전적, Ranking, 시즌
- 관전과 Replay
- Text/Voice Chat
- Tournament
- 방 비밀번호
- Rematch
- 2명 초과 대전
- PvP 전용 Emoji와 금지/제한 목록

## 17. 구현 순서 제안

1. 현재 Emoji Registry에서 PvP 사용 가능 목록과 Pool Validator를 만든다.
2. 싱글플레이와 분리된 `MultiplayerProfile`, `Room`, `Participant`, `Match` 상태를 정의한다.
3. 공용 전투 규칙을 두 참가자 ID로 실행할 수 있게 분리하고 Headless PvP Test를 만든다.
4. 빈 Board, HP 30, 양쪽 Draw 3개, Random 선공을 구현한다.
5. 서버에서 방 생성·참가·준비·시작과 상태 revision을 구현한다.
6. 서버 Turn Timer와 자동 배치를 구현한다.
7. 모드 선택, 프로필·Pool Builder, 방 참가, 대기방 UI를 연결한다.
8. 기존 전투 UI를 나/상대 표시와 원격 Turn에 맞게 연결한다.
9. 결과, 기권, 재접속, 방 정리를 구현한다.
10. 네트워크 지연, 중복 요청, 조작된 Pool, 동시 준비를 자동 테스트한다.

## 18. 완료 조건

- 싱글플레이를 선택하면 기존 흐름과 결과가 바뀌지 않는다.
- 유효하지 않은 닉네임이나 Pool로 방을 만들거나 참가할 수 없다.
- Pool의 10~15개, 동일 Emoji 2개, 고급 8개, 희귀 4개 제한을 클라이언트와 서버가 모두 검사한다.
- 유효한 코드로만 입장하며 존재하지 않음, 만석, 시작됨 오류가 구분된다.
- 두 명 모두 준비했을 때 전투가 정확히 한 번 시작된다.
- 두 플레이어의 시작 HP는 30이고 시작 Board는 비어 있다.
- 같은 Match seed로 선공, Draw, 자동 배치와 Random 효과가 재현된다.
- 현재 행동 플레이어만 15초 안에 유효한 행동을 보낼 수 있다.
- 시간 초과 시 서버가 남은 Draw와 빈칸을 선택해 정확히 한 번 자동 배치한다.
- 양쪽 화면의 Board, HP, 상태, Effect Event, Turn이 항상 같은 revision에 수렴한다.
- 기존 단일/다중 Bingo와 효과 순서가 PvP에서도 변하지 않는다.
- 정상 승패, 기권, 재접속 실패가 각각 올바른 결과로 끝난다.
- 결과 화면을 닫으면 방과 Match 구독을 정리하고 메인 화면으로 돌아간다.
- 모바일과 데스크톱에서 Pool 편집, 방 코드 입력, Board 배치가 가능하다.

## 19. 확정 정책

아래 항목은 초기 PvP 구현 규칙으로 확정한다.

| 항목 | 확정값 |
|---|---|
| 이벤트 전용 `event_egg`, `event_baby`의 Pool 선택 허용 | 제외 |
| 상대가 준비한 정확한 Pool 공개 | 대기방에서는 숨기고 결과 화면에서 공개 |
| 추가 배치의 제한 시간 | 배치 기회마다 새 15초 |
| 동시 HP 0 | 무승부 |
| 전투 중 재접속 유예 | 같은 Page Runtime에서만 30초, Turn Timer는 계속 진행 |
| Profile·Pool 저장 | 현재 Page의 App Memory에만 유지하고 DB와 Browser Storage는 사용하지 않음 |
| 메인·결과 화면 이동 후 재진입 | 같은 Page Runtime이면 직전 Profile·Pool을 미리 채움 |
| 새로고침·재실행 | 이전 Session 복구 없이 Profile·Pool 재설정 |
| 방 코드 | 혼동 문자 없는 6자리 |
| 대기방 만료 | 생성 후 30분 |
| PvP 전용 금지 Emoji | 초기에는 없음. 승률 자료를 본 뒤 추가 |
| Backend | Cloudflare Workers + Durable Objects + WebSocket Hibernation |

확정값은 서버 상태와 설정값으로 분리해, 향후 밸런스 정책을 바꾸더라도 Bingo Engine이나 Emoji 콘텐츠를 다시 작성하지 않게 한다.
